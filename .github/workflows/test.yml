name: CI

on:
  - push
  - pull_request

jobs:
  test:
    name: Node.js ${{ matrix.node-version }} on ${{ matrix.runs }}
    runs-on: ${{ matrix.runs-on }}-latest
    
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 15
          # LTS
          - 14
          - 12
    
        runs-on:
          - ubuntu
          - windows
          - macos
    
    steps:
      - name: 📫 Check mailbox
        uses: actions/checkout@v2

      - name: 🪂 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      
      - name: 📦 Obtain details
        run: npm install
      
      - name: 📋 Deliver prognosis
        run: npm test